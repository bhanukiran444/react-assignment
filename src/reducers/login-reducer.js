const inittialLoginState = {
  username: 'hruday@gmail.com',
  password: 'hruday123'
};

const loginReducer = (state = inittialLoginState, action) => {
  return state;
};

export default loginReducer;
